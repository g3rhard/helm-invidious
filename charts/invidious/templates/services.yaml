{{- range $svcName, $svcValues := .Values.services }}
{{- if $svcValues.enabled }}
{{- if or (ne $svcName "companion") (and (eq $svcName "companion") ($.Values.companion.enabled | default true)) }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-%s" (include "invidious.fullname" $) $svcName }}
  labels:
    app: {{ include "invidious.name" $ }}
    chart: {{ $.Chart.Name }}
    release: {{ $.Release.Name }}
    service: {{ $svcName }}
spec:
  type: {{ $svcValues.type }}
  ports:
    - name: http
      port: {{ $svcValues.port }}
      targetPort: {{ $svcValues.port }}
  selector:
    {{- if eq $svcName "materialious" }}
    app: {{ include "invidious.name" $ }}-materialious
    component: materialious
    {{- else if eq $svcName "companion" }}
    app: {{ include "invidious.name" $ }}-companion
    component: companion
    {{- else }}
    app: {{ include "invidious.name" $ }}
    {{- end }}
    release: {{ $.Release.Name }}
{{- if $svcValues.loadBalancerIP }}
  loadBalancerIP: {{ $svcValues.loadBalancerIP }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
